for d in data :
    print(d)
in 뒤에 라이브러리도 올 수 있음

for i in range(10) :
    pirnt(i, end =' ')

# 자주 실수하는 것
for문 끝에 ':' 와 tab 누락(자바와 달리 바디의 구분('{}')이 없기 때문에 들여쓰기가 중요하다.)

리스트 값이 이상하다면 리스트를 튜플로 바꿔보자
    튜플은 수정이 불가능하기 때문에 밑에 변경되는 코드가 있을 경우 에러 발생

if문은 else가 있어야 논리적 오류가 없음
    else에 뭐적어야할 지 모를 때는 #TODO를 넣어두기